# ID do cliente OAuth do Google, necessário para o botão de login com Google.
# Obtido no Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=seu-client-id.apps.googleusercontent.com

# URL da API do backend (usado apenas em produção).
# Em desenvolvimento, o Vite faz proxy de /api para localhost:8000 automaticamente.
# API_URL=https://seu-dominio-api.com
